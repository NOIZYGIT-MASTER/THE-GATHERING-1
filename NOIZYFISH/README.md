# NOIZYFISH

**Class:** NOIZY BRAND (per `docs/architecture/NOIZY_TAXONOMY_v1.md`)
**Authority:** RSP_001 · 2026-04-28
**Status:** scaffold · awaiting vacuum-and-sort distribution

## Purpose

Canonical home for everything classified as **NOIZYFISH** under the THE-GATHERING monorepo.

Inputs flow through `_vacuum/manifest.json` (vacuum-and-sort engine output).

## Sub-classification

Within NOIZYFISH, content is sorted by:
1. **Subject matter** (`docs/`, `code/`, `audio/`, `design/`, `research/`)
2. **Date of creation** (`YYYY/MM/` prefix in filenames or top-level subdirs)

## Boundary (hard)

- No A/V media on the system drive — externals only (per `feedback_no_av_system_drive.md`).
- This dir holds **manifests and pointers** to media stored in THE-AQUARIUM / R2.
- Source content (`*.md`, `*.html`, `*.json`, design files, code) lives here directly.

