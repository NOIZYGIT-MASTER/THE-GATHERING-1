# Claude Session — agent-ad2f545a9e5151b05

_First: 2026-04-03T22:36:37.547Z  ·  Last: 2026-04-03T22:39:11.696Z_

**Source:** `/Users/m2ultra/.claude/projects/-Users-m2ultra-Desktop-CLAUDE-TODAY-ANTIGRAVITY/e1467ab6-ffeb-409b-8e4e-d7d2581b0583/subagents/agent-ad2f545a9e5151b05.jsonl`

---

## user

Build the NCP (NOIZY Consent Protocol) middleware for the NOIZY.AI server. This is the consent enforcement layer that answers "who owns this?" for every data flow.

The project is at /Users/m2ultra/Desktop/CLAUDE TODAY/ANTIGRAVITY/noizy-ai/

Create a new file: src/middleware/consent.ts

The middleware should:

1. Export a `ConsentCheck` interface:
```typescript
interface ConsentCheck {
  actorId: string;        // who is making the request
  resourceType: string;   // what type of resource (voice, music, data, identity)
  action: string;         // what action (create, read, update, delete, clone, distribute)
  targetId?: string;      // whose resource is being accessed
  consentToken?: string;  // optional consent token for pre-authorized actions
}
```

2. Export a `ConsentResult` interface:
```typescript
interface ConsentResult {
  allowed: boolean;
  reason: string;
  neverClauseViolation?: string;  // which NC_ clause was violated
  auditId: string;               // UUID for the audit trail
  timestamp: string;
}
```

3. Import the HVS.NEVER_CLAUSE_CODES from '../shared/constants.js'

4. Export an async function `checkConsent(check: ConsentCheck): Promise<ConsentResult>` that:
   - Generates a UUID for the audit trail
   - Checks if the action violates any Never Clauses (map action types to clause codes)
   - If the actor is accessing their OWN resource (actorId === targetId), always allow
   - If the actor has a valid consentToken, allow (for now, just check it's non-empty)
   - Otherwise, deny with reason "No consent granted"
   - Logs every check (allow AND deny) using createLogger from '../shared/logger.js'

5. Export a function `createConsentToken(actorId: string, grantedTo: string, resourceType: string, actions: string[], expiresAt: Date): string` that creates a base64-encoded JSON token

6. Export a function `validateConsentToken(token: string): { valid: boolean; actorId: string; grantedTo: string; resourceType: string; actions: string[]; expiresAt: string } | null` that decodes and validates

Also, add a consent check endpoint to the server. Read /Users/m2ultra/Desktop/CLAUDE TODAY/ANTIGRAVITY/noizy-ai/src/server.ts and add:

- `POST /api/consent/check` — accepts a ConsentCheck body, returns ConsentResult
- `POST /api/consent/token` — creates a consent token (requires actorId, grantedTo, resourceType, actions, expiresInHours)
- `POST /api/consent/validate` — validates a token

Import the consent functions at the top of server.ts. Add the endpoints BEFORE the SUPERSONIC section.

Write clean TypeScript. No placeholder code. No TODO stubs. This is production.

## assistant



Let me read the existing codebase first.

## assistant


`[tool: Read]`


## assistant


`[tool: Read]`


## assistant


`[tool: Read]`


## user


`[tool result]`


## user


`[tool result]`


## user


`[tool result]`


## assistant

Now let me check the middleware directory exists.

## assistant


`[tool: Bash]`


## user


`[tool result]`


## assistant


`[tool: Bash]`


## user


`[tool result]`


## assistant

Now I'll create the consent middleware and update the server.

## assistant


`[tool: Write]`


## user


`[tool result]`


## assistant

Now update server.ts -- add the import and consent endpoints before the SUPERSONIC section.

## assistant


`[tool: Edit]`


## user


`[tool result]`


## assistant


`[tool: Read]`


## user


`[tool result]`


## assistant


`[tool: Edit]`


## user


`[tool result]`


## assistant



The imports are unused because I haven't added the endpoints yet. Let me find the right insertion point and add them now.

## assistant


`[tool: Read]`


## user


`[tool result]`


## assistant


`[tool: Edit]`


## user


`[tool result]`


## assistant


`[tool: Read]`


## user


`[tool result]`


## assistant


`[tool: Edit]`


## user


`[tool result]`


## assistant

Let me verify the build compiles.

## assistant


`[tool: Bash]`


## user


`[tool result]`


## assistant

Clean compile, zero errors. Here is what was built:

**New file: `/Users/m2ultra/Desktop/CLAUDE TODAY/ANTIGRAVITY/noizy-ai/src/middleware/consent.ts`**

The NCP middleware implements:

1. **`ConsentCheck` and `ConsentResult` interfaces** -- the protocol's core data contracts.

2. **`checkConsent()`** -- the async enforcement function that evaluates every request through a 4-step pipeline:
   - **Never Clauses first** -- maps action+resourceType pairs (e.g. `clone:identity`, `transfer:voice`) to `NC_` violation codes from `HVS.NEVER_CLAUSE_CODES`. These are absolute denials, no override possible.
   - **Self-access** -- if `actorId === targetId`, always allowed (you own your own data).
   - **Consent token validation** -- decodes the token and verifies the grantee, resource type, permitted actions, and expiration.
   - **Default deny** -- no consent means no access.
   - Every check (allow and deny) is logged with a UUID audit trail via pino.

3. **`createConsentToken()`** -- produces a base64-encoded JSON token containing actorId, grantedTo, resourceType, actions, expiresAt, and issuedAt.

4. **`validateConsentToken()`** -- decodes, validates required fields, checks expiration. Returns typed payload or null.

**Modified file: `/Users/m2ultra/Desktop/CLAUDE TODAY/ANTIGRAVITY/noizy-ai/src/server.ts`**

Three new endpoints added before the SUPERSONIC section:

- **`POST /api/consent/check`** -- accepts `ConsentCheck` body, returns `ConsentResult`
- **`POST /api/consent/token`** -- creates a token (requires actorId, grantedTo, resourceType, actions[], expiresInHours)
- **`POST /api/consent/validate`** -- validates a token, returns decoded payload or `{ valid: false }`

All endpoints include input validation with 400 responses for missing fields. TypeScript compiles clean with zero errors.
