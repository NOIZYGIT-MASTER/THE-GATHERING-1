{
  "name": "gabriel",
  "version": "0.1.0",
  "private": true,
  "description": "GABRIEL Control Plane Worker — policy-governed Cloudflare control.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "jose": "^5.9.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260401.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.100.0"
  }
}
